export enum Statuses {
  LOADING = "loading",
  SUCCESS = "success",
  ERROR = "error",
}

export enum Sizes {
  XXS,
  XS,
  S,
  M,
  L,
  XL,
  XXL,
  XXXL,
}

export enum SortBy {
  PRICE_ASC = "price_asc",
  PRICE_DESC = "price_desc",
}
